Map	Button B1		Page=Main			{{ curpage }}
Map	Button B2		Page=Sample			{{ curpage }}
Map	Button B3		Page=Grains			{{ curpage }}
Map	Button B4		Page=Pitch			{{ curpage }}
Map	Button B5		Page=Osc			{{ curpage }}
Map	Button B6		Page=Mixer			{{ curpage }}
Map	Button B7		Page=Amp			{{ curpage }}
Map	Button B8		Page=Effects			{{ curpage }}
