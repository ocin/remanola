Map	Button B1		Page=Main			{{ curpage }}
Map	Button B2		Page={{ oscpage }}			{{ curpage }}
Map	Button B3		Page={{ filterpage }}			{{ curpage }}
Map	Button B4		Page=AmpEnv			{{ curpage }}
Map	Button B5		Page={{ modpage }}			{{ curpage }}
Map	Button B6		Page={{ perfpage }}			{{ curpage }}
Map	Button B7		Page=Reverb			{{ curpage }}
Map	Button B8		Page=Delay			{{ curpage }}
