Map	Button B1		Page=Main			{{ curpage }}
Map	Button B2		Page=Osc1			{{ curpage }}
Map	Button B3		Page=Osc2			{{ curpage }}
Map	Button B4		Page=Noise			{{ curpage }}
Map	Button B5		Page=Filter			{{ curpage }}
Map	Button B6		Page=Filter2			{{ curpage }}
Map	Button B7		Page={{ modpage }}			{{ curpage }}
Map	Button B8		Page={{ perfpage }}			{{ curpage }}
