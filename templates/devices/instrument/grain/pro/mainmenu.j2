Map	Button B1		Page=Main			{{ curpage }}
Map	Button B2		Page=Sample			{{ curpage }}
Map	Button B3		Page={{ oscpage }}			{{ curpage }}
Map	Button B4		Page={{ amppage }}			{{ curpage }}
Map	Button B5		Page=Filter			{{ curpage }}
Map	Button B6		Page={{ modpage }}			{{ curpage }}
Map	Button B7		Page={{ perfpage }}			{{ curpage }}
Map	Button B8		Page=Effects			{{ curpage }}
