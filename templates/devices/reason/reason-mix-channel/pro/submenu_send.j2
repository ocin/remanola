Map	Button A1		FX1 Send On			{{ curpage }}
Map	Button A2		FX2 Send On			{{ curpage }}
Map	Button A3		FX3 Send On			{{ curpage }}
Map	Button A4		FX4 Send On			{{ curpage }}
Map	Button A5		FX5 Send On			{{ curpage }}
Map	Button A6		FX6 Send On			{{ curpage }}
Map	Button A7		FX7 Send On			{{ curpage }}
Map	Button A8		FX8 Send On			{{ curpage }}
