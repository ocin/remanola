Map	Button B1		Page=Main			{{ curpage }}
Map	Button B2		Page=Level			{{ curpage }}
Map	Button B3		Page=Pan			{{ curpage }}
Map	Button B4		Page=Meter			{{ curpage }}
Map	Button B8		Page=AuxSend			{{ curpage }}
